machine:
  node:
    version: 9.3.0
  post:
    - node -v
    - npm -v

dependencies:
  override:
    - npm install

test:
  override:
    - npm run build
    - npm run test:jest
    - npm run test:standard
